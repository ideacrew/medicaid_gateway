<h1>Account Transfers To <%= @transfers.first&.service.upcase if @transfers.any? %></h1>
<label for="start_on">Start On:</label>
<input type="date" id="start_on" name="start_on" value="<%= @start_on %>" data-reflex="input->Report#change_date" data-session="atp_start">
<label for="end_on">End On:</label>
<input type="date" id="end_on" name="end_on" value="<%= @end_on %>" data-reflex="input->Report#change_date" data-session="atp_end"></input>
<table class="table table-striped table-responsive"  style="table-layout:fixed; width:100%; word-wrap: break-word;">
	<thead>
		<th>Transfer Status</th>
		<th>Created At</th>
		<th>Family HBX Id</th>
		<th>Application HBX Id</th>
		<th>Ingestion Status</th>
		<th></th>
	</thead>
	<tbody>
		<% @transfers.each do |transfer| %>
			<tr>
				<td class="<%= transfer.successful? ? 'table-success' : 'table-danger' %>"><%= transfer.successful? ? "Success" : "Failure" %></td>
				<td><%= transfer.created_at %></td>
				<td><%= transfer.family_identifier %></td>
				<td><%= transfer.application_identifier %></td>
				<td>
					<% if transfer.service =="curam" && transfer.callback_status.blank? %>
						<div data-reflex="click->Report#check_payload" data-id="<%= transfer.id %>">
							<button class="btn btn-secondary">Check Payload</button>
						</div>
					<% else %>
						<%= transfer.callback_status %>
					<% end %>
				</td>
				<td><%= link_to "details", transfer, class: 'btn btn-primary' %></td>
			</tr>
		<% end %>
	</tbody>
</table>